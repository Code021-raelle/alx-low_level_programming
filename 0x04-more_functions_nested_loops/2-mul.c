#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: mul.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}

